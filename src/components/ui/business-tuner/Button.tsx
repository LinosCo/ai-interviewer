/**
 * Thin alias for the marketing/business-tuner context.
 * All logic and styling live in the unified Button at @/components/ui/button.
 * Consumers using fullWidth, withShimmer, or any variant continue to work unchanged.
 */
export { Button, type ButtonProps } from '@/components/ui/button';
